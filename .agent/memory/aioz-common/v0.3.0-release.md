---
type: project
tags: [aioz-common, release]
created: 2026-09-10
agent: main
---

# aioz-common v0.3.0 release (prepared 2026-09-10, not tagged)

- The user asked for the v0.3.0 note to cover the WHOLE history, not just v0.2.0..HEAD
  (v0.1.0 and v0.2.0 folded in). Repo note: `.gitlab/releases/v0.3.0.md`; vault:
  `Projects/aioz-common/releases/v0.3.0.md` + `v0.3.0-review.md`, indexed from
  `Projects/aioz-common/INDEX.md` (folder created for this release, full-path wikilinks).
- The whole-module review verdict was **fix-first**: 17 findings. Majors: lrucache `Add` never
  removes evicted list element (unbounded growth); rabbitmq lost-channel confirm read as a
  NACK and not retried; consumer handlers dead-letter on DrainTimeout during normal operation;
  a slow Graylog blocks all logging; `config.SaveConfig` writes secrets into config.yaml.
- History quirk: the repo has 21 root commits (history imports), so "since the start"
  means `git log HEAD`, not `root..HEAD`.
- hermes vault writes used `cx/gpt-5.6-sol` (the pinned `kr/` route is still dead, see
  [[hermes-kiro-credentials-dead]]).
