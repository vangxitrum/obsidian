# depin (AIOZ DePIN hub) — memory index

Project: `/home/tuan/work/depin-workspace/depin` — AIOZ DePIN decentralized storage network.

- [[ci-release-drift-fixes]] — 3 CI/release-model drifts fixed on feat/cicd (coverage rules inheritance, `:latest` moved from `main` to tag pipeline w/ rc guard, `develop` build/publish asymmetry) + `docs/RELEASE.md` rewrite; plan-gap found+user-approved mid-run; `.gitlab-ci.yml` header comment flagged not fixed; full rules/ref-simulation/bake.sh dry-run verification green
- [[hub-overview]] — what the DePIN hub is: Storj-modeled encrypted distributed object storage (coordinator/worker/edge/uplink).
- [[segment-verify-plan]] — plan (not yet built) for a standalone `cmd/segment-verify` on-demand segment health checker; carries the load-bearing gotchas: Storj's `Exists` RPC is dead so no worker change is needed, order limits must be minted PER PROBE (usedserials keys on coordinator+serial, not piece id), a successful Fetch can return 0 bytes, `CreateAuditOrderLimits` silently drops offline holders, concurrency can't follow Storj's 1000 through a shared relay IP, and any coord-adjacent CLI needs `memory://` revocation or it deadlocks on the coordinator's bolt flock
- [[delete-contract-shipped]] — DeleteContract (soft-delete contract + hard-delete files/segments) on feat/delete-contract, uncommitted; TxRunner deviation user-approved; go-sdk local replace pending restore
- [[deletion-testplanet-suite]] — 15 e2e delete tests (section 18 DEL) shipped on feat/implement-testcases, uncommitted; found + fixed the duplicate migration 000033 that broke ALL testplanet on develop, and vo.GrpcError's missing Forbidden case (auth failures returned Internal, not PermissionDenied); worker_tags migration still needs hand-applying to local coord-db

Design specs/plans + component docs moved out of the repo into the Obsidian vault: `/home/tuan/personal/tuan/projects/depin/{specs,plans,docs}/` (index: `projects/depin/INDEX.md`). Repo `docs/` now holds only generated files.
- [[coord-split-compose]] — multi-container coord compose: per-role containers + init-identity/migrate one-shots, identity via config/volume
- [[delete-file-phase1]] — DeleteFile metadata-delete Phase 1 implemented on worktree branch feat/delete-file, uncommitted; Phase 2 (GC) pending
- [[worker-autoupdate]] — storj-faithful worker auto-update (cmd/worker-updater, versioncontrol rollout cursor, checker client); e2e verified; fixed 2 pre-existing pkg/version bugs (build-panic w/o git tag, semver String() leading-dot)
- [[edgeserver-go-sdk-migration]] — edgeserver+testplanet migrated uplink-sdk→go-sdk (require+replace pinned pseudo-version); hit+fixed gogo-protobuf duplicate RegisterEnum panic (go-sdk vendored pb collided with depin's own) via go-sdk commit bb4f12b3ecdc; full testplanet suite green
- [[edgeserver-metadata-headers]] — edgeserver serves file metadata as HTTP headers (system headers direct + X-User-Meta-*); go-sdk WithHeaderSink + DownloadManifest proto resync; gotchas: restore infectious addmul_amd64.s (gitignore drop), piece_key.json field rename ({"private_key":...}), RegisterMapType only logs; e2e in testplanet passed; go.mod replace still local ../go-sdk pending finalize
- [[coord-missing-committed-ttl-gc]] — BUG: coord never GCs committed objects whose TTL expired (expireddeletion only clears PENDING uploads); workers TTL-drop pieces, coord keeps holder rows forever -> phantom holders in /segment-holders/; fix = add committed-TTL cleanup chore
- [[worker-hashstore-pieces-endpoint]] — read-only worker debug endpoint GET /hashstore-pieces/ lists pieces held in the hashstore (paged max-heap over Tbl.Range, coord filter); worker counterpart to coord /segment-holders/; gotcha: vo.PieceID.String() is base32 (order != bytes); debug server built before backend so lazy closure
- [[monitor-server-stack]] — receiver side of the push pipeline: turnkey monitoring/ docker-compose (Prometheus remote-write-receiver + Grafana provisioned dashboards + Caddy bearer/TLS); verified e2e; gotcha: out_of_order_time_window is config-file only, not a CLI flag
- [[telemetry-remote-write-push]] — pkg/telemetry Prometheus remote_write push client (Task 1, commit 5ca6ea6) + wired into every coord role incl. relay (Task 2, commit c8c973d), both on branch feat/coord-metrics-collection; off-by-default guard must precede NewClient() itself (not just lifecycle registration); AIOZ_METRICS_* env vars confirmed via `coord setup` round trip; lifecycle.Group has no item-introspection API, test via zaptest/observer log capture or real httptest.Server round trip
- [[coord-audit-metrics-instrumentation]] — Task 3 (commit 0f04a9f, same branch): monkit outcome counters + verify duration in coord/audit's Worker.auditOne and ReverifyWorker.processJob; reused existing `mon` var (lives in observer.go, not a dedicated monkit.go); new test-isolation pattern for monkit producer code (swap package `mon` var to a scoped `monkit.NewRegistry()` in a same-package test file, `withTestMon(t)` helper)
- [[coord-repair-metrics-instrumentation]] — Task 4 (commit c138c7e, same branch): monkit counters/gauge/duration across coord/repair's checker→queue→repairer pipeline; all 3 sub-packages already had `mon`; repair_queue_length via queue's existing Count(ctx); real outcome sites for success/failed/strategy/pieces-reconstructed live in repairer/segments.go not repairer.go; tests include a genuine RS encode/reconstruct round trip and a goroutine-join pattern for race-free monkit assertions on a polling loop
- [[coord-order-metrics-instrumentation]] — Task 5 (commit 8548ebe, same branch): monkit counters for order signing (order_limits_signed/signing_errors, tagged action=PieceAction.String()) in Signer.Sign() (coord/order/signer.go, the one real per-piece signing site shared by all Create*OrderLimits callers), and settlement (order_settlement_bytes/rows) per-completed-row in SettlementWithWindow (coord/order/settlement.go); coord/order/endpoint.go is nearly empty (not where RPC logic lives — SettlementWithWindow is in settlement.go); tag pattern matches worker/piecestore/endpoint.go's action-tag idiom; test needed a hand-rolled fake grpc.ClientStreamingServer stream + grpcpeer.NewContext to inject a real mTLS peer identity. Review-fix follow-up (commit 448ec8d): settlement counters now tagged by action too, + test coverage for the store-write-failure `continue` path (fakeSettlementStore.errFor per-action selective failure)
- [[coord-file-metrics-instrumentation]] — Task 6 (commit 3670b02, same branch): monkit counters for coord/file/endpoint.go's real create/commit/download success sites: file_created(_bytes) in CreateFile (no kind tag, segment kind unknown yet), segment_committed(_bytes) tagged kind=remote/inline in CommitSegment/MakeInlineSegment (the codebase's existing 2-RPC split reused as the tag), segment_downloaded(_bytes) tagged kind=remote/inline in buildDownloadSegment (reused its existing `len(seg.InlineData)>0` branch); disclosed limitation: CommitSegment/MakeInlineSegment are idempotent but the endpoint can't see "real write vs retry no-op" so retries double-count; tests build a real overlay.Service/IdentityCache/DownloadSelectionCache/order.Service against narrow fakes (not endpoint mocks), CLONE algorithm sidesteps RS stripe math in test fixtures
- [[coord-accounting-metrics-instrumentation]] — Task 7 (commit 6349e9e, same branch): monkit counters/durations for coord/accounting/tally (tally_duration, tally_objects_tallied, tally_bytes_tallied, at Service.Tally's real completion site, deliberately alongside a pre-existing pre-branch monAccounting.IntVal total_objects/total_bytes block left untouched) and coord/accounting/workertally (worker_tally_workers_tallied counter + worker_tally_payout_bytes observation, at Service.Run's completion site after SaveWorkerTallies succeeds; this package had zero prior instrumentation); both already had `mon`, no new monkit.go; eventkit.go untouched per brief
- [[coord-overlay-metrics-instrumentation]] — Task 8 (commit 81dca60, same branch): monkit gauges/duration for coord/overlay's UploadSelectionCache and DownloadSelectionCache; new coord/overlay/monkit.go (package had none); metrics gated on refresh success only. Review-fix (commit 7f9b67e, same day): download cache DOES have Vetted per worker (same projectWorker path as upload's split) - added download_cache_reputable_workers/new_workers, eligible now their sum
- [[coord-gc-metrics-instrumentation]] — Task 10 (commit fa93b32, same branch): monkit counters/observations for coord/gc/bloomfilter (bundles_built, pieces_per_filter, filter_size_bytes at Observer.Finish) and coord/gc/sender (retain_success/retain_fail at the Retain RPC outcome, via a new `retain()` helper split out of `send()` so tests mock at the generated piecepb.PieceStoreServiceClient interface boundary); both packages already had `mon`
- [[coord-monitoring-vps-artifacts]] — Task 11 (final task, same branch): monitoring/README.md + 3 Grafana dashboards (coord-overview/coord-storage-lifecycle/coord-integrity), pure static VPS artifacts, no repo code; corrected two field-name errors in the task brief by reading pinned monkit source (Counter field is "value" not "count"; average field is "ravg" not "avg"; environment.Register measurements are NOT prefixed "environment_"); reputation has only one metric (reputation_worker_disqualified, no suspend metric); ranged-loop metrics are pre-existing (ranged_loop_stats, ranged_loop_suspicious_segments_count, rangedloop_observer_duration)
- [[coord-metrics-final-review-fixes]] — final whole-branch-review polish (commit 551e753, same branch): repair_strategy_rs/_clone consolidated into one tagged repair_strategy counter; order_settlement_bytes dashboard panel switched ravg->sum+rate; $role template var (dead dropdown in all 3 dashboards) wired into all 79 panel queries via role=~"$role" regex matcher (required because $role is multi+includeAll); reusable pattern for bulk-editing Grafana JSON via Python regex-inject + json.dump(indent=2)
- [[coord-metrics-collection-shipped]] — feat/coord-metrics-collection: all 11 tasks + final review done, pushed to origin, MR link + human naming/scope decisions + main-divergence note
- [[depin-gitignore-allowlist-gotcha]] — allowlist-style .gitignore silently drops new non-.go assets (embed files, .s asm, new dirs); only surfaces as go build failure on a truly fresh clone, not existing checkouts
- [[ticket-tags-usage]] — SUPERSEDED by [[contract-file-tags-usage]]; kept for jsonb GROUP BY + egress-billing + go-sdk symlink gotchas
- [[contract-file-tags-usage]] — S3-faithful contract tags (bucket) + file tags (object) + GetUsageByContractTag/GetUsageByFileTag RPCs; branch feat/ticket-tags-usage (uncommitted); coord/tags leaf package (import-cycle fallback), file_storage_finalized survives file delete; also notes a phantom "relay observability" memory (uncommitted work lost in a torn-down worktree)
- [[worker-portable-package]] — `fleet.sh package` builds a source-less worker zip (Docker-only install); control/coord baked into image ENV (Dockerfile ARG + entrypoint fallback), init.sh loads image + mints via dockerized keytool; `tcp:` coord-parse fix in preflight (fleet.sh + init.sh); workers-only, amd64; e2e verified 2026-07-19, uncommitted
- [[edge-outbound-bandwidth]] - edge-only outbound-egress cuts: canonical `/download/{fileId}` CDN route + strong file-id ETag/304 + gzip + closed-range 206; go-sdk out of scope caps Range (no size edge-only); build needs `../go-sdk` symlink; testplanet e2e blocked by pre-existing go-sdk drift; uncommitted
- [[coord-getdownloadinfo-range]] - Phase 1 of true ranged downloads: coord GetDownloadInfo/ByTicket accept a PlainRange, return stripe-exact range-scoped manifest (covering segments only, scoped order limits + egress/allocated, plain_offset/piece_offset); coord-only, go-sdk/edge deferred; pieceByteRange stripe/block math; unit-tested green; uncommitted
- [[gosdk-ranged-download]] - Phase 2: go-sdk consumes the range-scoped manifest - DownloadByTicketRange/DownloadFileRange + OpenRange (fetch piece_offset window -> RS decode -> discard to block -> decrypt from firstBlock -> trim); proto resync (targeted, not the wipe-all sync script); testplanet drift fixed + TestRangedDownload e2e green; gotchas: formatter mangles go-sdk common import, LSP stale for symlinked go-sdk; uncommitted
- [[edge-http-range]] - Phase 3 (FINAL): edge serves true HTTP Range - go-sdk ResolveTicketRange (returns manifest+FileSize) + edge parseRange (closed/open/suffix) -> real 206/Content-Range: s-e/total/Content-Length via DownloadManifestRange; serveHead (Content-Length), writeResolveError (grpc->HTTP), NewFromClientConfig; removed old stream-and-trim; TestEdgeRangedDownload e2e green; ranged-download feature COMPLETE across all 3 phases; uncommitted
- [[relay-domain-locator-egress]] — cut relay→edge billed egress via a /dns4 worker locator + authoritative split-horizon DNS (Approach A; dual-address C deferred); one reserve.go change (selectAdvertisedLocator) preserves the domain into the signed order limit; spec+plan in vault, not yet implemented.
- [[relay-connected-peers-vs-reservations]] — Grafana "Connected peers" panel = raw libp2p conn count, not circuit reservations; gap explains ping-test 204 NO_RESERVATION failures, confirmed live via Prometheus 2026-07-21
- [[worker-tags-plan]] — SHIPPED 2026-08-04 (was: plan): Storj node tags ported → depin worker tags, merged into develop; foundation-first (advertise→verify→store→debug-read, placement still deferred) + full self-signed+authority signing; pkg/nodetag, worker_tags table, keytool sign-tags
- [[depin-test-plan]] — testing-phase test plan (docs/test-plan.md, uncommitted): 17 sections, 159 TC-ID rows, functional-only (benchmark/load-test rows removed by user request); new gap found: worker graceful exit has zero coverage
- [[logship-grafana-allvalue-bug]] — Loki rejects all-empty-compatible LogQL matchers (`=~".*"`); Logs Explorer dashboard's app/role/instance vars fixed .* -> .+; logship pipeline itself was fine; also notes real monitoring stack runs from work/depin-workspace not treehouse worktrees
- [[edge-download-timeout-fix]] — root-caused + fixed a live edge-server hang/goroutine-leak (406K goroutines, 11.4GB RSS, triggered by a modest 50-VU k6 HLS load test); no timeout past the 20s TCP dial anywhere in the download path; fix = ServerConfig.DownloadTimeout + downloadClient interface for testability; regression test verified to actually fail pre-fix
- [[grpc-pool-reuse-relay-regression]] — commit 79ae347 pool reuse funnels concurrent piece downloads through one limited relay circuit; dies after ~3 pieces (GOAWAY 4101); fix = no-reuse-over-limited + evict dead conns
- [[gosdk-segment-prefetch]] — SUPERSEDED: segment-prefetch API and pipeline removed 2026-08-03; downloads are strictly serial pending stripe-level streaming reconstruction
- [[relay-conn-reuse-ws2]] — WS2 edge download-speed: gated/OFF-by-default bounded reuse of relayed worker conns (pool LimitedReuseMaxStreams M) + worker rcmgr Conn/Peer stream-cap knobs + grpcconn keepalive; counter on poolValue (RawConn iface unchanged); WithLimitedRelayReuse option + edge config; -race tests green; BLOCKING: edgeserver needs go-sdk commit+re-pin to build; WS4 measurement still needed for a safe non-zero M
- [[client-billing-phase-a]] — prepaid USD balance funded by on-chain AIOZ deposits; period invoices debit it; deposit watcher; hex-vs-bech32 address trap; Phase B gating specified not built
- [[edge-k6-download-failures]] — k6 edge "download failed" = edge-emitted 502/504 (relayed piece streams reset under concurrency, segment 1 cannot reach RS threshold); plus truncated-200 silent corruption + 4 bench-harness bugs (VUS/DURATION dropped, report labels s as ms, failed-rate max-not-ratio, loadgen co-located with edge)
- [[download-failure-fixes-applied]] — TDD fixes for the edge/SDK download failures: edge aborts instead of truncating a 200, pool no longer closes in-use conns (+Capacity 2000), SDK re-stamps its own timeout as DeadlineExceeded; coord keepalive needed no change (fleet workers predate the fix); residual 20-way failure is now the 3s client keepalive ACK budget
- [[download-benchmark-harness]] — self-service download benchmark: dev/bench/download-test.sh (sdk + edge arms) publishes to Pushgateway -> Grafana "Testing / Download Benchmark"; barchart-vs-bargauge Grafana trap; monitoring/.env missing WORKER_METRICS_BEARER_TOKEN blocks docker compose
- [[segment-prefetch-quiescence-bug]] — ROOT CAUSE of 256MiB edge download failures: eestream inactivity watchdog (5s of flat per-piece progress) starts at segment OPEN, so a prefetched segment 2 that nobody is reading yet is killed as "quiescence"; only bites on slow links / multi-segment files
- [[go-sdk-download-error-trace]] - full edge-to-go-sdk download error path, stage prefixes, HTTP mapping, coordinator-resolve timeout gap, and local-replace deployment caveat
- [[live-client-registrations]] - public IDs for Go SDK identities registered with the live coordinator
- [[download-network-cap-root-cause]] - ~9MiB/s direct ceiling is the shared public worker-relay path; edge halves it via full-segment store-and-forward across two serial legs
- [[worker-holepunch-never-upgrades]] — fleet workers can never hole-punch: Docker bridge, NO published port, double NAT rewrites source port (relay sees 118.69.133.193:ephemeral not :7777); libp2p HolePunchAddrs filters to public-only so only a per-flow SNAT addr qualifies; worker->relay direct conn already exists (direct_peers:1), the zero is direct_UPGRADES
- [[gosdk-streaming-reconstruction]] — RS pieces stream directly into stripe reconstruction with atomic segment-level K+6 reservations; deployed edge reaches 4.74-5.29MiB/s and uses a 512 global cap
- [[stripe-streaming-review]] — final streaming review: EOF over-fetch and replacement deadlock fixed; silent streams and worker-incompatible progressive orders remain
- [[edge-load-test-production-contention]] — 8x80MiB production-edge load test starves normal small-object admission: only 6 of the normal ~10 windows remain; latency 5x, 92% of increase is pre-piece queueing, CPU is low
- [[request-capacity-calculator]] — interactive standalone HTML model for target RPS across admission, cache, relay/network, CPU, coordinator, and proxy constraints; observed/load/10-RPS presets
- [[edge-request-admission]] — edge capacity is configured as concurrent body downloads; excess gets retryable 503, while SDK/libp2p stream budgets are derived internally
- [[vps-capacity-sizing]] — measured 1-vCPU/4GB combined edge+relay planning envelope, bandwidth/transfer math, and scaling thresholds
- [[testplanet-coverage-plan]] — plan to execute docs/test-plan.md's 159 rows via testplanet; feat/implement-testcases was 104 commits behind develop; ~35 rows already green at package level (deposit/updater/versioncontrol/relay); the 5 structural reasons testplanet cannot cover ~25 rows; TestEdgeserverMetadataHeaders is a false green; Postgres-skip-exits-0 trap
- [[component-version-tags]] — per-component build version + repo Source tag (storj bake.sh model) on feat/comopent-tags, uncommitted; traps: bare `export` in Makefile.migration expands every `define` macro, allowlist .gitignore hides scripts/, file-list builds disable Go vcs stamping; also found the 50 live fleet workers' auto-update has been inert since 2026-07-24
- [[fleet-host-tag]] — fleet `HOST_TAG` -> self-signed `host=<machine>` worker tag (entrypoint/gen-fleet/init.sh/Dockerfiles); traps: `fleet.sh package` ships a STALE bin/worker unless you `make build-*` first, entrypoint's unconditional `cp -f` wipes an auto-updated /data/bin/worker; plus proof that fleet auto-update is inert everywhere (no versioncontrol server) and v0.0.7 is a 31-Jul leftover
- [[docker-bake-release-pipeline]] — docker buildx bake multi-platform release + GitLab CI (ghcr.io + Docker Hub, provenance/cosign); Zig cross-compile, static musl; macOS needs a native runner, linux/arm blocked upstream; 8 traps incl. dockerignore glob eating a source pkg and `git config --add` for insteadOf
- [[cfgstruct-bindable-types]] — cfgstruct binds only a closed set of leaf types; an `int32` config field panics in cmd `init()` and kills every role of the binary; panic message now names flag+field, jobq MaxAttempts fixed, bind guard test added
- [[worker-payout-aioz-conversion]] — worker payouts converted USD -> AIOZ at the treasury's lifetime volume-weighted deposit rate + client `GetAiozPrice` RPC (feat/reward-flows, uncommitted); carries the two-different-rates trap (clients quoted spot, workers paid the blend), the shopspring `DivisionPrecision=16` truncation two digits short of atto, and the `Config.Parse()` early-return that left the api role with a $0.00 fixed rate source
- [[worker-disk-capacity-findings]] — disk-full/almost-full tests TC-FS-14..17; order limit sized to max segment, burned serial makes retry inert, storageStatus never populated
- [[piece-key-expiration]] — Piece Ed25519 keys do not expire; 24h order limits do, while committed pieces have no TTL.
- [[worker-online-liveness]] — Worker online status derives from `last_seen` within the 4-hour liveness window; the stale `is_online` column was removed.
- [[order-metadata-worker-compat]] — Mixed coordinator/worker rollout after signed order metadata causes all workers to reject download order limits; deploy matching workers.
- [[testplanet-ci-hang-elimination]] — FIXED: the CI testplanet stall was LibP2PListener.Accept returning (nil,nil) on a closed channel, so grpc Serve spun forever and Run never returned (10s/planet); c8f831d only reduced how often shutdown reached it. Also a fixed GC bloomfilter race, coverage.sh -timeout, bake.sh ssh-vs-job-token gating
- [[go-aioz-confined-to-keytool]] — AIOZ chain codec moved behind an injectable `CosmosKeyTool.Codec`; worker 181M->57M, updater 174M->48M, keytool keeps go-aioz by design; unblocks 32-bit linux/arm but the `binaries` GROUP also needs `binaries-linux-arm` added, and the worker Docker image still ships keytool
- [[keytool-secrets-and-aioz-prefix]] — keytool now prints aioz1 (derivation proven unchanged) and takes no secret on argv (/dev/tty, not speakeasy/stdin); found the armor feature was entirely broken, plus a disabled replay window in pkg/auth that coord shares
- [[shutdown-preface-serveWG-stall]] — THIRD cause of the 10.2s testplanet shutdown stall: a conn accepted but never sent its 24-byte HTTP/2 preface pins grpc's serveWG, and stop() waits on serveWG before firing s.done, so Serve/Run never return and even the forced Stop cannot help; our eager-TLS + lazy-grpc.NewClient dialing manufactures exactly such conns. Fixed with grpcutil.TrackedListener force-closing conns after the graceful window in coord+worker. ALSO fixed the client-side cause: `dial.Conn.Close()` was a no-op because `pool.Conn` omitted `Close` and `dial.Conn` embedded a nil `io.Closer`, so no caller could ever release a dialed conn - worker/trust's force-dial of the coord was the socket that stalled it; causal-proved by disabling the server fix (20/20 green on the client fix alone). Carries the pprof-dump-on-blown-budget technique and two traps in writing the regression test
- [[download-retry-resume]] — Storj Layer-3 transparent mid-stream download retry in go-sdk + edge Content-Length/resume; carries the load-bearing blocker (combineErrs flattened the error chain with %v so NO retry policy could ever fire), the status.Code-returns-Unknown trap, the shared-stall-timer trap, the wire-vs-plaintext resume-offset trap, and the exact 3 conditions that make the e2e genuinely RED
- [[gosdk-coverage-70]] — go-sdk 40.6%->70.06% with new coverage tooling; 3 P0 blockers first (piecedownload goroutine leak panicking half of all runs and silently deleting coverage, PieceID.Scan nil-vs-zero, combineErrs); upstream storj test ports are the big lever; found a remote-triggerable panic in PeerIdentityFromChain, asymmetric tlsopts removeNils, broken UUID.Version, and a repo-deleting sync script
- [[period-close-and-gating]] — automatic monthly close for both ledgers + intra-period accrual watchdog + upload/download payment gate (402); on-by-default is only safe because of the first-run floor; found the `distributed` clobber on re-close and the permanently-frozen client charge
- [[download-phase-tracing]] — phase-level download tracing across edge/go-sdk/worker/relay; piece_open split + piece_first_byte + admission_wait + coord resolve; sdk_* monkit export for direct-SDK runs; X-Download-Id correlation; carries the classify-transport-after-the-dial rule, the two libp2p relay metrics that are NOT derivable, the ranged-path CPU-accounting lie, and the testplanet no-MetricsSink trap
- [[download-stats-storj-comparison]] — download-stats overstates a 64KiB eestream barrier; Storj uses the same K-of-N decoder, while AIOZ uniquely aligns 64KiB worker messages with grpc-go's unconfigured 64KiB HTTP/2 window; includes corrected 30MiB piece geometry and the 38-issued/35-active tail mismatch
- [[billing-settlement-audit-2026-08-20]] — live-DB audit of usage/charge + FIXED the settlement loss bug: `SendOrders` archived (destroyed) a whole hour's orders on ANY trust-resolve error, not just ErrUntrusted; settlement also lags ~49h BY DESIGN (OrderLimitGracePeriod 48h) which fakes a "settlement is dead" reading; plus egress billed at manifest-issue not delivery, GET_AUDIT never records allocated, `dead` blind to unsettled orders, no cross-window replay guard, 2026-08 charged mid-period and frozen by the charge-once index
- [[coord-admin-ui]] — admin back-office (`coord admin` peer + Vue SPA) shipped on feat/coord-admin-ui, uncommitted; plan narrowed to admin-only mid-session; carries the seed-admin-could-never-log-in bug that only manual verification caught, the httprouter static-vs-wildcard panic, vo.USD being attousd not dollars, node_modules leaking into the Go module, and the pre-existing uuid-ossp test race
- [[audit-pipeline-performance]] — traced the slow coord audit from Grafana alone (monkit function_times / segments = a per-segment cost budget); batched the per-holder reputation write, gated pooled dialer, batched queue claim, reverification_queue indexes; 4 traps incl. GORM silently zeroing embedded structs and reltuples=-1 on a fresh table
- [[buildkit-secret-not-inherited]] — release.Dockerfile build-binaries died with `no secure protocol found for repository`: a `type=secret` mount is NOT inherited by child stages (but the .gitconfig insteadOf rules ARE), and a recreated buildx builder drops the /go/pkg/mod cache mount while keeping the layer cache, so `go build` re-fetched the private module graph with no netrc; fix = mount the netrc in build-binaries too
- [[worker-console]] — local HTTP API for the DePIN Worker GUI (branch feat/worker-local-httpserver-for-UI, uncommitted); ported from go-aioz-media's local REST router; flag-gated, no auth, no CORS by design; traps: protobuf omitempty in JSON responses, space/shared.go never fills storageStatus, config_tool.go has never worked, bech32 needs RegisterBlockchainDefaultCfg
- [[depin-test-postgres]] — testplanet/coord-db tests skip SILENTLY without DEPIN_TEST_POSTGRES (a 0.2s "ok" is a skip); use depin-pgtest :15445, never coord-db :5445; full suite ~380s, run with nohup
- [[period-close-dashboard-heartbeat]] — admin dashboard Billing periods panel: last client charge + last worker payout + next-close countdown; needed a new `period_close_status` heartbeat table because the admin peer is a separate process with no periodclose config and sync2.Cycle has no next-run accessor; null next_due_at means "due now", not "unknown"
- [[coord-admin-readonly]] — admin back-office made structurally read-only vs coord: 4 mutating routes + writer interfaces + coord-write-url config deleted; AuthSupport==AuthViewer now; disqualify/reinstate has NO CLI replacement so overlay.DB is caller-less
- [[github-release-publish]] — public GitHub Releases as a second publish destination for the worker, plus the `aioz-depin-worker_<os>_<arch>.zip` operator bundle (`publish:github` + `publish-github.sh`); carries the require-checksum-is-still-impossible constraint and the worker/keytool CLI facts (worker `run` is a stub, `config storage` is dead code, keytool has no `identity` parent, identity-dir defaults disagree)
- [[transcode-build]] - transcode-as-a-service build: Phases 1-2 (protos, coord config/session, migration 000055) done on feat/transcoding; ffmpeg-as-subprocess + real-tables decisions; TRANSCODE still missing from the paidActions whitelist (Phase 3) or settlement silently fails
- [[gc-verified-working]] — GC confirmed working end-to-end on the live fleet (bloom build -> hourly retain -> hashstore compaction actually reclaiming GiBs); carries the two traps that produced wrong conclusions: `docker logs` on fleet workers only spans ~76 min, and monkit `field="count"` is not the call count (use `field="total"`); open items = 52 compactOnce errors/24h, worker logs absent from Loki. 2026-09-10 (bug/worker-trash): "trash never reclaimed" disproved by a record-level hashtbl dump - 0 trash past expiry, trash just lives 7 days (ExpiresDays) by design
- [[updater-leftover-staging-wedge]] — 27/50 workers never updated: a container recreate mid-update left `/data/bin/worker.v<ver>` and the O_EXCL unpack failed "file exists" forever (Storj has the same pattern); fixed by removing the leftover first. TRAP: "hasn't made it to this node yet" is the updater's SELF-update check, not the worker's
- [[worker-storage-numbers]] — worker used-space numbers match du to 146 KB, but SharedDisk.DiskSpace never fills storageStatus (Storj does) so Total/Free=0 everywhere (console, coord disk_space_free, admin UI) and available is never capped by real free disk: transcribe claims 189.9 GB free on a host with 13.8 GB; `sum(used_space)` without field="recent" is garbage
