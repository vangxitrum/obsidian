# aioz-stats — memory index

Project: `/home/tuan/work/templates/aioz-stats` — library-only Go module (`gitlab.internal/tuan.quang.tran/aioz-stats`) providing the monkit debug/observability HTTP server, a `version.Info` build stamp and a test context helper.

- [[aioz-stats-testcontext-check]] — `testcontext.Check` calls its function immediately; it is not a deferred closer.
