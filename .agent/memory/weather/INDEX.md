# weather — memory index

Project: `/home/tuan/work/map/weather` — self-hosted weather service (point data + GRIB2 tiles).

- [[self-hosted-weather-service]] — architecture + confirmed decisions (Python, self-host Open-Meteo, S3+RabbitMQ, click-to-value by raster sampling); ports from aioz-map/parser-service.
